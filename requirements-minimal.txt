# RuView Absolute Minimal Requirements — Zero compilation
# Only packages with pre-built pure-Python wheels for MINGW64

# Web framework + ORM
fastapi>=0.95.0
uvicorn>=0.20.0
pydantic>=2.0.0  # Required by app code (field_validator is v2 only)

# Testing
pytest>=7.0.0

# CLI utilities
click>=8.0.0
